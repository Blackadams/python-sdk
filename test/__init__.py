import os

# auth params
api_key = os.getenv('api_key', 'fake')

# product params
app_id = os.getenv('app_id', 'fake')
product_id = os.getenv('product_id', 'fake')
messaging_short_code =  os.getenv('messaging_short_code', 'fake')